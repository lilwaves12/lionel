name: job
on:
  push:
   
jobs:
  first_job:
    name: "first job"
    runs-on: ubuntu-latest
    steps:
      - name: "first job"
        run: |
          echo "ceci est le premier job"
          
  second_job:
      name: "second job"
      runs-on: ubuntu-latest
      steps:
        - name: "second job"
          run: |
            echo "ceci est le second job"
  third_job:
        name: "third job"
        runs-on: ubuntu-latest
        steps:
          - name: "third name"
            run: |
              echo "ceci est le troisierme job"

          

          
          
